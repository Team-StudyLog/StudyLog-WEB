const examplePages = {
    EXAMPLE: '/example',
}

const homePages = {
    HOMEPAGE: '/',
}

const myPages = {
    MYPAGE: '/mypage',
}

const authPages = {
    LOGINPAGE: '/login',
    KAKAO_REDIRECTION: '/kakao/redirection',
}

export default {
    ...examplePages,
    ...homePages,
    ...myPages,
    ...authPages,
}