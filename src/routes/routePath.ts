const examplePages = {
    EXAMPLE: '/example',
}

const homePages = {
    HOMEPAGE: '/',
}

const myPages = {
    MYPAGE: '/mypage',
}

export default {
    ...examplePages,
    ...homePages,
    ...myPages,
}